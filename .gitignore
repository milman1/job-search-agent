node_modules/
.env
*.log

# Personal data used by the auto-apply engine — never commit real PII.
profile.json
resume.txt
# Résumé documents (any PDF/DOCX) — treated as personal data.
*.pdf
*.docx

# Generated cover letters (contain resume-derived content).
cover-letters/
